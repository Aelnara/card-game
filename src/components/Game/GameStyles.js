import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
   Game: {
      width: '100%',
      height: '100vh',
      position: 'relative',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      overflow: 'hidden'
   }
})
import { makeStyles } from '@material-ui/core/styles'

export default makeStyles({
   Hand: {
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
      alignItems: 'center',
      position: 'absolute',
      bottom: '-5rem'
   }
})